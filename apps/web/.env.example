# NestJS BFF / API gateway base URL (server-side only — never exposed to client)
# In Docker the web container reaches the API over the internal network:
#   API_BASE_URL=http://api:3901
# Running web on the host (outside Docker), point at the published API port:
API_BASE_URL=http://localhost:3901
